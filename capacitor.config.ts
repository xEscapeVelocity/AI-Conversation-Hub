import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aiconversationhub.app',
  appName: 'AI Conversation Hub',
  webDir: 'dist'
};

export default config;
